language:
  - objective-c 

compiler:
  - gcc
  - clang

script:
  - mkdir build
  - cd build
  - CXXFLAGS="-D__extern_always_inline=inline" cmake -DCMAKE_BUILD_TYPE=Debug ..
  - cmake --build .

notifications:
  irc: "irc.freenode.org#unvanquished-dev"
  irc: "irc.quakenet.org#unvanquished"
  on_success: change
  on_failure: always

branches:
  except:
    - debian
    - engine-upgrade/sound
